<!DOCTYPE html>
<html>
  <head>
    <title>Pinboard</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'https://maps.googleapis.com/maps/api/js?key=AIzaSyA6LI_hichjUjuNQFPQ-UAtRHVrpeA1aKw' %>
    <%= javascript_pack_tag 'places', 'data-turbolinks-track': 'reload' %>
    <%= yield(:head_tags) %>
  </head>

  <body>
  <%# if :authenticate_user! == false %>
  <% if user_signed_in? %>
    <p><%= link_to 'EXIT not Gogl', destroy_user_session_path, method: :delete, data: { confirm: 'Do you want to exit?' } %>
      Current user : <%= current_user.email %>
    </p><% elsif user_signed_in_? %>
    <%= link_to 'EXIT G', logout_path,  data: { confirm: 'Do you want to exit?' } %>
    Current user: <%= current_user_.email %>
  <% else %>
    <a href="/auth/google_oauth2">Sign in with Google</a>
    <%= link_to 'Sign in with our acc', user_session_path %>
    <% end %>
    <%= link_to 'See all users', users_path %>
    <%= link_to 'See all advertisements', advertisements_path %>
    <p class="notice"><%#= notice %></p>
  <p class="alert"><%#= alert %></p>
  <%= yield %>
  </body>
</html>
